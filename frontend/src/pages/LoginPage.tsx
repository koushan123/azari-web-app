import { useState, type FormEvent } from "react";
import { useAuth } from "../auth/AuthContext";
import { Field } from "../components/ui";
import { useRouter } from "../routes/router";
export function LoginPage() { const { login } = useAuth(); const { navigate } = useRouter(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false);
  const submit = async (event: FormEvent) => { event.preventDefault(); setBusy(true); setError(""); try { await login(email, password); navigate("/dashboard", true); } catch { setError("ایمیل یا رمز عبور صحیح نیست. دوباره تلاش کنید."); } finally { setBusy(false); } };
  return <main className="login-page"><section className="login-intro"><div className="login-mark">آ</div><p className="eyebrow">سامانه مالی یکپارچه</p><h1>حسابداری روشن،<br />تصمیم‌گیری مطمئن.</h1><p>اسناد مالی، گزارش‌های مدیریتی و تحلیل هوشمند کسب‌وکار شما در یک محیط امن و ساده.</p><div className="trust-list"><span>✓ حسابداری دوطرفه</span><span>✓ گزارش‌های واقعی</span><span>✓ تحلیل هوشمند قابل توضیح</span></div></section><section className="login-panel"><form onSubmit={submit} className="login-form"><header><h2>ورود به حساب کاربری</h2><p>برای ادامه، مشخصات حساب خود را وارد کنید.</p></header>{error && <div className="alert alert--error" role="alert">{error}</div>}<Field label="نشانی ایمیل"><input type="email" dir="ltr" autoComplete="username" value={email} onChange={(e) => setEmail(e.target.value)} required autoFocus /></Field><Field label="رمز عبور"><input type="password" dir="ltr" autoComplete="current-password" value={password} onChange={(e) => setPassword(e.target.value)} required /></Field><button className="button button--primary button--large" disabled={busy}>{busy ? "در حال ورود…" : "ورود به سامانه"}</button><small className="secure-note">اطلاعات ورود شما با ارتباط امن به سرور ارسال می‌شود.</small></form></section></main>;
}

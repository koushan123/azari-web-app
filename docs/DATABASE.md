# Database

PostgreSQL 16 is the primary store. The normalized schema, constraints, indexes,
and migration history will be introduced in the persistence milestone. Money is
stored as fixed-precision decimal values and posted journals must balance.


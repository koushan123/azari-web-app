# Machine Learning

The project implements TF-IDF classification (Naive Bayes/SVM comparison),
Random Forest payment risk, Prophet cash-flow forecasting, and standardized
K-Means segmentation. Training is offline and reproducible; inference uses
versioned persisted artifacts. Evaluation results will be generated, not
hardcoded, and synthetic results will be labeled as demonstration results.


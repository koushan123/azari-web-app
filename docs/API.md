# API

The API is rooted at `/api/v1`; interactive OpenAPI documentation is served at
`/docs`. The Stage 1 liveness route is `GET /api/v1/health`. Domain and ML routes
will be added with typed request/response schemas in subsequent milestones.

